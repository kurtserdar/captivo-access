node_modules/
.next/
out/
dist/
*.log
.env
.env.local
.env.*.local
coverage/
.DS_Store
next-env.d.ts
.turbo/
prisma/*.db
*.tsbuildinfo
src/generated/

# Internal design/planning docs — never enter the public repo (kept on the team's disk)
docs/design/

# Go build artifacts (compiled binaries from `go build ./...`)
/dataplane/dataplane
/connector/connector

# Local dev compose override (e.g. host-port tweaks) — not committed
docker-compose.override.yml

# Local Claude Code tooling / third-party skills — not part of the project
.claude/
